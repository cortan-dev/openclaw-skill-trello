## Summary
- add native Anthropic support to the PR review script
- keep OpenAI-compatible support as a fallback path
- update workflow messaging to explain either credential path clearly

## Changes
- add `AnthropicClient` using the Anthropic Messages API
- select Anthropic when `ANTHROPIC_API_KEY` is present
- keep `OPENAI_API_KEY` + `OPENAI_BASE_URL` support for compatible providers
- update workflow env wiring for `ANTHROPIC_API_KEY` and `ANTHROPIC_BASE_URL`
- improve skip messaging when no LLM credential is configured

## Configuration
Preferred:
- repository secret: `ANTHROPIC_API_KEY`

Optional overrides:
- repository variable: `ANTHROPIC_BASE_URL`
- repository variable: `ASSISTANT_REVIEW_MODEL`
- repository variable: `SPARTAN_REVIEW_MODEL`

Default Anthropic model:
- `claude-sonnet-4-5`

## Notes
- Anthropic is preferred when both credentials are present
- this keeps the existing review comment/update flow unchanged
